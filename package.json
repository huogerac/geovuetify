{
  "name": "opendata4nature",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "preinstall": "npx only-allow npm",
    "postinstall": "nuxt prepare",
    "clean": "git clean -xdf node_modules .nuxt .output",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "eslint src/"
  },
  "dependencies": {
    "nuxt": "^3.14.1592",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
